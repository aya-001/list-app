<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>LIST APP</title>
    <link rel="stylesheet" href="/css/style.css">
    <script src="/send_url.js"></script>
  </head>
  <body>
    <header>
      <a href="/" class="header-logo">LIST</a>
    </header>
    <div class="container">
      <div class="container-header">
        <h1>Edit Item</h1>
        <a href="/index" class="cancel-button">Cancel</a>
      </div>
      <div class="item-form-wrapper">
        <p class="form-label">Item</p>
        <form action="/update/<%= ShoppingList.id %>" method="post">
          <!-- ↑Specify the destination URL and the submit method  for the form based on the routing-->
          <input name="itemName" value = "<%= ShoppingList.name %>" type="text">
          <input name="itemNumber" value = "<%= ShoppingList.number %>" type="text">
          <!-- Specify the name attribute -->
          <input type="submit" value="Update">
        </form>
      </div>
      <a href="/index" class="cancel-button">Go back</a>
    </div>
  </body>
</html>